{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field%}

    <div>
        <img src="{{ asset('uploads/'~ object.imageName | default('no_image.jpg')) }}" alt="image"
             width="200" height="100">
    </div>
{% endblock %}